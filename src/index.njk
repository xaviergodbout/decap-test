---
layout: base.njk
eleventyComputed:
  title: "{{ home.title }}"
---

<h1>{{ home.title }}</h1>

{{ home.body }}

{% if home.hero_image %}
<img src="{{ home.hero_image }}" alt="Hero image">
{% endif %}