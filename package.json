{
  "name": "my-eleventy-site",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run update-nav && eleventy",
    "start": "npm run update-nav && eleventy --serve",
    "update-nav": "node scripts/updateNavigation.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.1"
  },
  "dependencies": {
    "markdown-it": "^14.1.0"
  }
}
